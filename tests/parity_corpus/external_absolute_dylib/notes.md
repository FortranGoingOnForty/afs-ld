External absolute-symbol parity case. Separate objects define, alias, and
reference one fixed value. Data bytes, symbol records, exports, and partitions
are compared with Apple `ld` under dead stripping.
